// Service Worker for offline caching
// This is automatically generated by next-pwa but we're documenting the strategy

self.addEventListener('install', (event) => {
    console.log('Service Worker installing...');
});

self.addEventListener('activate', (event) => {
    console.log('Service Worker activated');
});

// The @ducanh2912/next-pwa package automatically handles:
// - Static asset caching (CSS, JS, images)
// - Runtime caching for API responses
// - Offline fallback pages
// - Workbox precaching strategies

// Custom offline handling can be added here if needed
self.addEventListener('fetch', (event) => {
    // Let next-pwa handle this automatically
});
